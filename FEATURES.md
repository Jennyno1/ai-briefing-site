# 特性清单（FEATURES）

按优先级排列。每条给出目标、设计、演进路径与约束；状态随后续迭代更新。

## F1 · 动态个性化配置（待实现）

**目标**：读者按自身关注方向获得个性化的落地行动与赛道视图；站点数据保持单源（一份 JSON，多视图）。

**背景**：目前所有读者看到同一份全量视图。而「落地行动」天然带受众属性（写给做企业 IT 的人、写给做内容创作的人、写给个人效率玩家），赛道关注度也因人而异。

**设计**

- 新增 `site.config.json`（人群画像配置，进仓库、可审计）：
  - `focus_tracks`：赛道子集与排序；
  - `action_tags`：给每条 action 打受众标签（如 `企业IT` / `内容创作` / `个人效率`）；
  - `featured_topics`：置顶主题。
- 构建侧按配置给数据打标（schema 新增**可选** `audience` 字段，向后兼容）。
- 前端默认全量渲染；支持 `?profile=xxx` 按画像过滤与高亮——**只做过滤与排序，不做服务端**。

**演进路径（三步，步步可用）**

1. 第一步：赛道显隐 + 排序配置化（零 schema 变更，纯前端 + 配置）。
2. 第二步：actions 受众标签（需在日报模板里让模型输出标签，落到 `audience` 字段）。
3. 第三步：多画像多入口（同一份数据，多个 `?profile=` 入口）。

**约束**

- 配置文件进仓库、可审计；不得引入服务端与用户追踪；
- 不破坏既有 JSON 的必填 schema（老数据仍可读）；
- 个性化只做「过滤 + 排序 + 高亮」，不改写内容。

## F2 · 数据增量更新与闸门（已实现，持续加固）

每日只追加当天 JSON + manifest；发布前过硬门禁与发布集自检；失败不发布并留待处理。

## F3 · 单日深链与分享（已实现）

`#YYYY-MM-DD` 哈希深链（点击日期即更新地址栏）、键盘 ↑↓ 切换日期、Enter/空格打开、跳转正文链接。

## F4 · 无障碍与可读性（已实现，持续）

`h1/h2` 语义层级、焦点可见、跳转正文、`prefers-reduced-motion`、打印样式、暗色链接对比度达 WCAG AA。

## F5 · 更多视图形态（待评估）

按赛道聚合的月度视图、按主题聚合的专题页；GitHub 项目的星数趋势。评估前提：不引入构建链与服务端。
